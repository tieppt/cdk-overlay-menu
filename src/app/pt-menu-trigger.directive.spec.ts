import { PtMenuTriggerDirective } from './pt-menu-trigger.directive';

describe('PtMenuTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new PtMenuTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
